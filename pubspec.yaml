name: logger_barta
description: A structured, colorful logger for Flutter. Logs APIs, WebSockets, errors, cURL commands, and debug prints. Includes an in-app viewer and is release-safe.
version: 0.1.4
homepage: https://github.com/soft-ash/ash-logger
repository: https://github.com/soft-ash/ash-logger
issue_tracker: https://github.com/soft-ash/ash-logger/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
